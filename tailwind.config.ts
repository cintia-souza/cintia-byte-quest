// tailwind.config.ts
const config = {
  theme: {
    extend: {
      colors: {
        brand: {
          blue: "#0047FF", // Azul da logo
          dark: "#050505", // Fundo quase preto
          silver: "#F5F5F7", // Branco para textos
        },
      },
    },
  },
};
export default config;
